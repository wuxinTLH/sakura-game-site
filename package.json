{
    "name": "sakura-game-site",
    "private": true,
    "scripts": {
        "dev": "npx concurrently \"npm --prefix frontend run dev\" \"npm --prefix backend run dev\"",
        "dev:frontend": "npm --prefix frontend run dev",
        "dev:backend": "npm --prefix backend run dev"
    },
    "devDependencies": {
        "concurrently": "^8.0.0"
    }
}